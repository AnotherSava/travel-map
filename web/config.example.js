// Local config (gitignored). Copy this file to `config.js` and set a MapTiler key
// to load the base map; the in-page switcher offers MapTiler's Streets, Satellite
// hybrid, and Light styles. Get a free key at https://cloud.maptiler.com/ → Account
// → Keys. Without a key the app shows a prompt instead of a map.
window.MAPTILER_KEY = "";

// Optional: force a specific base style URL (any MapLibre style JSON), bypassing
// the switcher default.
// window.MAP_STYLE = "https://tiles.openfreemap.org/styles/liberty";
