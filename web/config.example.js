// Local config (gitignored). Copy this file to `config.js` and set a Mapbox token
// to load the base map; the in-page switcher offers Mapbox's Streets, Satellite
// hybrid, Light, and Dark styles. Get a free token at https://account.mapbox.com/
// (Tokens). Without a token the app shows a prompt instead of a map.
window.MAPBOX_TOKEN = "";

// Optional: force a specific style URL (mapbox://... or any Mapbox-style JSON),
// bypassing the switcher default.
// window.MAP_STYLE = "mapbox://styles/mapbox/dark-v11";
