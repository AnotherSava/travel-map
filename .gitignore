# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Web local config (real Mapbox token — never commit)
web/config.js

# Owner-only enriched trip source — plaintext, never committed. The encrypted
# backup + shipped artifact is web/data/visits.enc (tracked).
web/data/visits.source.json

# Deploy staging (generated by scripts/build_site.mjs)
dist/

# Wrangler local cache (Cloudflare Pages deploys)
.wrangler/

# Secrets (.env.example is the committed template)
.env

# OS / editor
.DS_Store
Thumbs.db
